def solution(array, commands):
    answer = []
    for command in commands:
        data=array[command[0]-1:command[1]]
        data.sort()
        answer.append(data[command[2]-1])
    return answer
